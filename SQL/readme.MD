# 🗄️ SQL

This folder contains my SQL practice completed during my Databricks training. It includes basic to intermediate SQL concepts with hands-on examples and assignments.

## 📚 Topics Covered

### 🔹 SQL Commands

- `SELECT`
- `INSERT`
- `UPDATE`
- `DELETE`
- `MERGE`

### 🔹 DDL (Data Definition Language)

- `CREATE`
- `ALTER`
- `DROP`
- `TRUNCATE`
- `RENAME`

### 🔹 DML (Data Manipulation Language)

- `INSERT`
- `UPDATE`
- `DELETE`
- `MERGE`

### 🔹 DQL (Data Query Language)

- `SELECT`

### 🔹 DCL (Data Control Language)

- `GRANT`
- `REVOKE`

### 🔹 TCL (Transaction Control Language)

- `COMMIT`
- `ROLLBACK`
- `SAVEPOINT`

---

## 🔍 SQL Operations

### 📌 Filtering
- `WHERE`
- `DISTINCT`
- `LIKE`
- `BETWEEN`
- `IN`
- `NOT IN`
- `IS NULL`
- `IS NOT NULL`

### 📌 Sorting
- `ORDER BY`

### 📌 Grouping
- `GROUP BY`
- `HAVING`

### 📌 Limiting Records
- `TOP`
- `LIMIT`

### 📌 Aggregate Functions
- `COUNT()`
- `SUM()`
- `AVG()`
- `MIN()`
- `MAX()`

### 📌 Comparison Operators
- `=`
- `!=` / `<>`
- `>`
- `<`
- `>=`
- `<=`

### 📌 Logical Operators
- `AND`
- `OR`
- `NOT`

### 📌 Joins
- `INNER JOIN`
- `LEFT JOIN`
- `RIGHT JOIN`
- `FULL OUTER JOIN`
- `CROSS JOIN`
- `SELF JOIN`

### 📌 Set Operators
- `UNION`
- `UNION ALL`
- `INTERSECT`
- `EXCEPT`

### 📌 String Functions
- `UPPER()`
- `LOWER()`
- `LENGTH()`
- `SUBSTRING()`
- `CONCAT()`
- `TRIM()`
- `REPLACE()`

### 📌 Numeric Functions
- `ROUND()`
- `CEILING()`
- `FLOOR()`
- `ABS()`

### 📌 Date Functions
- `CURRENT_DATE`
- `CURRENT_TIMESTAMP`
- `DATEADD()`
- `DATEDIFF()`
- `EXTRACT()`

### 📌 Window Functions
- `ROW_NUMBER()`
- `RANK()`
- `DENSE_RANK()`
- `LEAD()`
- `LAG()`
- `NTILE()`

### 📌 Subqueries
- Single-row Subqueries
- Multi-row Subqueries
- Correlated Subqueries

### 📌 Common Table Expressions (CTEs)
- Simple CTE
- Recursive CTE

### 📌 Views
- Create View
- Update View
- Drop View

### 📌 Constraints
- `PRIMARY KEY`
- `FOREIGN KEY`
- `UNIQUE`
- `NOT NULL`
- `CHECK`
- `DEFAULT`

### 📌 Indexes
- Create Index
- Drop Index

---

## 🎯 Goal

This repository serves as my SQL learning journal during my Databricks training, helping me strengthen SQL fundamentals and prepare for Data Engineering interviews through consistent practice.
